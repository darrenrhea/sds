from setuptools import setup

setup(
    name="first_order_jets",
    version="0.1.0",
    py_modules=[
        "first_order_jets",
    ],
    license="MIT",
    long_description=open('README.md').read(),
    long_description_content_type="text/markdown",
    install_requires=[
    ],
    entry_points={
        "console_scripts": [
        ],
    },
)
