# custom_pytorch_datasets

This is a private Python library that you should install like this:

```bash
cd ~/r
git clone git@github.com:darrenrhea/custom_pytorch_datasets
cd ~/r/custom_pytorch_datasets
pip install -e . --no-deps
```
